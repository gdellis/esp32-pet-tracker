name: CI

on:
  push:
    branches: [main]
    paths:
      - 'firmware/**'
      - '.github/workflows/**'
      - '.pre-commit-config.yaml'
  pull_request:
    branches: [main]
    paths:
      - 'firmware/**'
      - '.github/workflows/**'
      - '.pre-commit-config.yaml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # Check if firmware build is needed
  check-firmware-changed:
    name: Check Firmware Changed
    runs-on: ubuntu-latest
    outputs:
      firmware_changed: ${{ steps.changed.outputs.firmware_changed }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Check firmware changes
        id: changed
        run: |
          if [[ "${{ github.event_name }}" == "push" ]]; then
            git diff --name-only HEAD~1 HEAD | grep -qE '^firmware/' && echo "firmware_changed=true" >> $GITHUB_OUTPUT || echo "firmware_changed=false" >> $GITHUB_OUTPUT
          else
            git fetch origin ${{ github.base_ref }} --depth=1
            git diff --name-only origin/${{ github.base_ref }} HEAD | grep -qE '^firmware/' && echo "firmware_changed=true" >> $GITHUB_OUTPUT || echo "firmware_changed=false" >> $GITHUB_OUTPUT
          fi

  # Lint Markdown and Shell via pre-commit (clang-format excluded - handled separately)
  lint-precommit:
    name: Lint (pre-commit)
    runs-on: ubuntu-latest
    continue-on-error: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache pre-commit
        uses: actions/cache@v4
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ runner.os }}-${{ hashFiles('.pre-commit-config.yaml') }}
          restore-keys: |
            pre-commit-${{ runner.os }}-

      - name: Install pre-commit
        run: pip install pre-commit

      - name: Run pre-commit (markdown + shell only)
        run: |
          pre-commit run markdownlint-cli2
          pre-commit run shellcheck

  # Lint C++ with clang-format dry-run
  lint-cpp:
    name: Lint C++
    runs-on: ubuntu-latest
    continue-on-error: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-clang-${{ hashFiles('.github/workflows/ci.yml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install clang-format
        run: |
          pip install --break-system-packages clang-format

      - name: Run clang-format dry-run
        run: |
          find firmware/main -type f \( -name '*.cpp' -o -name '*.hpp' -o -name '*.h' \) \
            -print0 | xargs -0 clang-format --dry-run --Werror 2>&1 \
            || echo "clang-format warnings (ESP-IDF uses GNU style)"

  # Build ESP-IDF firmware (runs in Docker)
  build-firmware:
    name: Build Firmware
    runs-on: ubuntu-latest
    needs: check-firmware-changed
    if: needs.check-firmware-changed.outputs.firmware_changed == 'true'
    container: espressif/idf:v6.0

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache ccache and ESP-IDF
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/ccache
            ~/.espressif
          key: esp32-ccache-${{ github.sha }}
          restore-keys: |
            esp32-ccache-

      - name: Build firmware
        run: |
          . /opt/esp/idf/export.sh
          ccache -M 100M
          ccache -z
          cd firmware
          idf.py build
          ccache -s

      - name: Upload build artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: firmware-build
          path: firmware/build
          retention-days: 1

  # Host-based unit tests
  host-tests:
    name: Host Tests
    runs-on: ubuntu-latest
    needs: check-firmware-changed
    if: needs.check-firmware-changed.outputs.firmware_changed == 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache CMake
        uses: actions/cache@v4
        with:
          path: ~/.cache/cmake
          key: ${{ runner.os }}-cmake-${{ hashFiles('firmware/tests/CMakeLists.txt') }}
          restore-keys: |
            ${{ runner.os }}-cmake-

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: host-ccache-${{ github.sha }}
          restore-keys: |
            host-ccache-

      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y cmake build-essential ccache

      - name: Configure & Build tests
        run: |
          ccache -M 50M
          ccache -z
          cd firmware/tests
          mkdir -p build && cd build
          cmake ..
          make -j$(nproc)
          ccache -s

      - name: Run tests
        run: |
          cd firmware/tests/build
          ./test_nmea_parser
          ./test_button_handler
          ./test_led_driver
          ./test_lora_driver
          ./test_geofence
          ./test_ble
